const JWT_SECRET = require('./jwt-secret');
const MAIL_CREDENTIAL = require('./mail-credential');
const FILES_CHECK = require('./files-check');

module.exports = {
    JWT_SECRET,
    MAIL_CREDENTIAL,
    FILES_CHECK
};